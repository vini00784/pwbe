// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// model tbl_usuario {
//  id Int @id @default(autoincrement())
//  nome String
//}

// Para realizar o primeiro acesso ao banco é importante criar uma model e verificar se ela ela é criada no BD
// Precisa ser feito pelo menos durante a primeira vez, para saber se o prisma tem acesso ao BD.
  // npx prisma migrate dev
